# Decisions Log: Access Control

Agent reports on completed tasks. Each entry is written by the agent that executed the task.

---

## Task 1: Миграция схемы доступа

**Status:** Done
**Commit:** 77b5cb4 (migration), f7e35b4 + ec5b31c (verification harness per review)
**Agent:** migration-writer
**Summary:** Added `server/migrations/004_access_control.up.sql` — `role`/`has_access`/`email`/`display_name` on `users`, Yandex identity columns on `oauth_sessions`, the `access_requests` table, the `(role, has_access)` index, and seeding of the two administrators. The SQL is byte-identical to the tech-spec Data Models block; every statement is idempotent (`IF NOT EXISTS` plus `INSERT ... ON DUPLICATE KEY UPDATE`), so a manual re-run neither fails nor duplicates admin rows. Because this is pure SQL with no Go test runtime, the TDD anchor is an executable check harness kept in `logs/working/task-1/`, written before the migration and confirmed to fail on the pre-004 schema.

**Deviations:** None in the migration itself. One addition beyond the task's "only the migration file changes": three verification scripts were committed under `logs/working/task-1/` (`verify_004.sh`, `reapply_004.sh`, `scenario_b.sh`) at the test-reviewer's request, so the checks behind the acceptance criteria are reproducible rather than existing only as pasted terminal output. They live in the feature work directory, not in `server/`, so the migration remains byte-identical to the spec for anyone diffing it.

**Reviews:**

*Design round 1 (before the SQL was finalised):*
- test-reviewer (design): 5 major + 1 minor, all fixed → [logs/working/task-1/test-reviewer-design-1.json](logs/working/task-1/test-reviewer-design-1.json)

*Code round 1:*
- code-reviewer: 3 minor, non-blocking → [logs/working/task-1/code-reviewer-1.json](logs/working/task-1/code-reviewer-1.json)
- security-auditor: no blockers; 1 low (new) + 1 medium (carried over from the tech-spec round) → [logs/working/task-1/security-auditor-1.json](logs/working/task-1/security-auditor-1.json)
- test-reviewer (full): PASS, 2 minor harness fixes applied → [logs/working/task-1/test-reviewer-1.json](logs/working/task-1/test-reviewer-1.json)

**Notes carried forward:**
- **For Task 4 (`EnsureUser`) — collation.** `users.id` has no explicit `COLLATE` and inherits the database default `utf8mb4_uca1400_ai_ci`, which is case- and accent-insensitive. Admin identity therefore depends on an ambient server setting rather than an explicit one, and Decision 11 has no normalisation step. Inherited from migration 002, benign for the two current logins (verified live: `'rogogdbd'` collides with the seeded row instead of creating a second admin). Raised by the security auditor as a tech-spec/task-level decision, deliberately not patched inside this migration.
- **Admin state asymmetry.** `ON DUPLICATE KEY UPDATE role = 'admin'` intentionally does not touch `has_access`, so an administrator who had already logged in keeps `has_access=0` while a freshly inserted one gets `1`. Both pass the gate, since access is `has_access || role=='admin'` (Decision 10).
- **Pattern caveat.** `ADD CONSTRAINT chk_users_role` validates every existing row at `ALTER` time, unlike a plain `ADD COLUMN`. Trivial at current data volume; worth remembering before reusing this pattern on a large table.
- **Public repo exposure — resolved, accepted.** `github.com/RoGogDBD/PoshivOn` is public (confirmed via the GitHub API: `private: false`), so the two seeded admin Yandex logins are permanently and publicly visible in git history, irreversibly. Not an auth bypass (a login is not a credential), but it publishes a confirmed two-account target list for phishing. Raised by the security auditor as low severity; surfaced to the user directly (outside this task's own review flow, since the finding only reached decisions.md after Task 1 had already closed). **User decision: accept as-is.** Documented as an explicit trade-off in tech-spec.md's Risks table rather than left as an unweighed byproduct of Decision 4. No code or migration change; repo visibility is a GitHub setting, out of this feature's scope.
- Checked and holding, no action: no SQL injection surface (static file, no interpolation, confirmed by full read); `role` and `status` are `NOT NULL`, so the CHECK-vs-NULL concern cannot arise — verified live that `role=NULL` fails with 1048 before any CHECK runs, and an out-of-enum value fails with 4025; FK and `ON DELETE CASCADE` verified live (orphan insert rejected 1452, cascade delete leaves no orphans); idempotency additionally confirmed under a simulated mid-file crash (first two `ALTER`s applied, then the whole file re-run) — no error, no duplication.

**Verification:**
- `verify_004.sh` — 22 schema assertions (column types/nullability/defaults, CHECK clause text, tightly scoped FK + `ON DELETE CASCADE`, PK, index column order, admin cardinality and case-sensitive identity) → all pass. Confirmed failing on the pre-004 schema first.
- Scenario A, clean volume, `docker compose up -d db app` → no `Ошибка применения миграций`; `schema_migrations` contains `004_access_control`; exactly 2 admins.
- Scenario B, production redeploy (`001`-`003` pre-recorded with live user rows, real `migrate.go` applying `004` alone) → all assertions pass; pre-existing rows backfill to `role='user'`, `has_access=0`, never NULL; `created_at` preserved.
- `reapply_004.sh` — manual re-run of the same file: client exit 0, no `ERROR` output, state unchanged, admin count still 2, no duplicated schema objects.

---

## Task 2: Доменный сервис доступа

**Status:** Done
**Commit:** 4191929 (service + tests), 804e010 + 718626b (review fixes)
**Agent:** access-service-writer
**Summary:** Added `server/internal/service/access.go` — the domain types (`Role`, `AccessState`, `UserRecord`, `AccessRequest`), the `UserRepository`/`AccessRequestRepository` interfaces verbatim from the tech-spec Data Models block, and `AccessService` with `EnsureUser`, `GetAccessState`, `CreateRequest`, `SetAccess`; `ErrForbidden` and `ErrConflict` joined the existing domain errors in `costing.go`. Decision 10 lives in exactly one unexported helper, `hasEffectiveAccess`, which both `GetAccessState` and `CreateRequest` go through, so nothing anywhere checks the raw `has_access` alone. The repository signals "zero rows affected" (Decision 5) by returning `ErrConflict` from `CreateRequest` — the signature is fixed by the spec, so the contract had to travel through the error, and the service wraps with `%w` so `errors.Is` survives.

**Deviations:** None from the task's "What to do". Three additions beyond the literal list, each from a review finding: `requireLogin` bounds the login by `users.id`'s column width (255, counted in runes so the boundary matches MariaDB's reading of `VARCHAR(255)`), `SetAccess` carries a doc comment stating it performs no authorization check of its own, and the test suite grew past the 10 TDD-Anchor cases to 20 top-level tests (36 leaf assertions incl. subtests) covering invalid arguments, infrastructure-failure paths, and the partial-failure ordering.

**Reviews:**

*Design round 1 (tests written, no implementation yet):*
- test-reviewer (design): 2 minor + 1 low + 1 informational, all addressed → [logs/working/task-2/test-reviewer-design-1.json](logs/working/task-2/test-reviewer-design-1.json)

*Code round 1:*
- code-reviewer: 3 minor, non-blocking → [logs/working/task-2/code-reviewer-1.json](logs/working/task-2/code-reviewer-1.json)
- security-auditor: 1 medium + 3 low + 2 verified-clean → [logs/working/task-2/security-auditor-1.json](logs/working/task-2/security-auditor-1.json)
- test-reviewer (full): PASS, findings array empty → [logs/working/task-2/test-reviewer-1.json](logs/working/task-2/test-reviewer-1.json)

**Notes carried forward:**
- **For Task 3 — the `SetAccess` snapshot race.** `SetAccess` decides whether to call `DecideRequest` from the `RequestStatus` it already read in `GetUser`, not from a fresh read. A second read would not close the gap, because nothing at this layer is transactional, so it would only add a query. Two concrete consequences when a `CreateRequest` interleaves: a request created after the snapshot stays `pending` forever (access has since been granted, so the next `CreateRequest` short-circuits on `ErrConflict` and never overwrites it), and in the revoke direction a request the user submits right after the flag is cleared can be stamped `approved`/`rejected` with this admin's `decided_by` though nobody reviewed it. `has_access` itself is never wrong — the flag is written with exactly the value the admin asked for — so this costs only the request row and the accuracy of the `decided_by` audit trail (Decision 5). The real fix is one transaction in the repository, which is Task 3's layer; whether a dangling `pending` row in the admin list is worth that is a product call nobody has made yet.
- **For Task 3 — no rollback across the two writes.** `SetAccess` writes the flag and then decides the request as two separate calls. A failure of the second surfaces an error to the caller with `has_access` already changed and not rolled back. This is now pinned by a test as deliberate current behaviour, not left implicit; making the pair atomic is again a repository-layer question.
- **For Task 4 — consume the rule, do not re-derive it.** `hasEffectiveAccess` is unexported on purpose: `GetAccessState(...).HasAccess` is the intended entry point and already returns the effective flag. Middleware that re-writes `has_access || role == 'admin'` in the handler package would put a second copy of Decision 10 in the tree and defeat the reason this task exists. Note that `AccessState.HasAccess` is the *effective* value while `UserRecord.HasAccess` stays the raw column — the handler is serialising the effective one, which keeps the tech-spec's client-side `has_access || role=='admin'` correct either way.
- **For Task 4 — `AccessService` has no `ListUsers`.** The repository interface has one (fixed by Data Models) but the service does not, since this task's method list did not include it. The admin-list endpoint will need one added to `AccessService` rather than reaching past it to the repository.
- **For Task 4/5 — two declared-but-unused members.** `ErrForbidden` is declared per spec and consumed by the middleware later; `AccessRequestRepository.GetRequest` may turn out genuinely unused, since neither `AccessState` nor `UserRecord` carries `DecidedBy`/`DecidedAt` and the admin-list description does not ask for decision metadata. Both are fixed by the spec, so neither was removed here — worth confirming they get used or recording them as reserved.
- Checked and holding, no action: no SQL or injection surface (the file has zero direct DB access, interfaces only); no user-enumeration or timing oracle (the login always comes from the caller's own session or an admin-only path); every error is wrapped with a fixed, non-parameterised prefix, so no repository detail reaches the caller through the message; `EnsureUser` cannot reset `role`/`has_access` structurally, since its signature has no such parameters (Decision 11).

**Verification:**
- Go is not installed on this host. Everything ran in the pre-existing `golang:1.25-alpine` image with `--network host` (the default bridge cannot reach `proxy.golang.org`) and a persistent module/build cache. Both the code-reviewer and the test-reviewer independently reproduced the suite the same way.
- `go build ./...` → clean, exit 0. `go vet ./...` → clean. `gofmt -l .` → no output.
- `go test ./internal/service/... -count=1` → `ok github.com/RoGogDBD/PoshivOn/internal/service`. All 20 `TestAccessService_*` top-level cases pass (36 leaf results with subtests); the 5 pre-existing `TestCostingService_*` are untouched and still pass.
- TDD order was real: the tests were written first and confirmed red against the missing implementation (build failed on undefined `UserRecord`/`RoleUser`/`AccessRequest`) before `access.go` existed.

---

## Task 3: Реализации репозиториев доступа

**Status:** Done
**Commit:** 61e9c51 (репозитории + контрактные тесты), 31b7076 + 698193f (правки по ревью, миграция 005)
**Agent:** repo-writer
**Summary:** `PostgresRepository` и `MemoryRepository` получили реализации `service.UserRepository` и `service.AccessRequestRepository`, а пакет — контрактный набор `access_repo_test.go`, который гоняет одни и те же утверждения по обоим хранилищам: по памяти всегда, по живой MariaDB при заданном `TEST_DB_DSN`. Колонки доступа читает и пишет отдельная GORM-модель `userAccessModel`, отображённая на ту же таблицу `users`; `userModel` остался с `ID`+`CreatedAt` и получил комментарий-инвариант (Decision 13). По итогам ревью сюда же вошла миграция `005_identity_collation.up.sql`, закрывающая присвоение чужой личности через регистронезависимую сортировку.

**Deviations:**
- **Именование контрактных функций.** TDD Anchor просил функции вида `TestAccessRepoContract_*(t, factory)`. Такая сигнатура не компилируется — `go test` требует у `TestXxx` ровно один параметр. Имена из Anchor остались на верхнеуровневых тестах, а параметр `factory` — на делегатах `contract*`, вызываемых дважды через `runAgainstBothStores`.
- **Четыре теста сверх списка Anchor:** `SetAccessRepeatedGrantIsNotNotFound` и `SetAccessRevokeWhenAlreadyRevoked` (MariaDB считает затронутой только реально изменённую строку, поэтому наивное «0 строк → ErrNotFound» превращало бы повторный клик администратора в 404), `GetRequestUnknownLoginNotFound` (ветка «заявки нет» иначе не покрыта) и `LoginIdentityIsCaseAndSpaceSensitive` (регрессия к миграции 005).
- **`MemoryRepository` получил `touchUserRow`** в трёх существующих путях записи (`UpsertSettings`, `CreateChat`, `AppendCalculation`). Это правка методов, которых задача не касалась, но без неё хранилища расходятся: в MariaDB строку пользователя заводит `upsertUser`, и без аналога в памяти такой пользователь выпадал бы из `ListUsers`.
- **Миграция 005** — не из плана задачи; добавлена по решению пользователя, см. ниже.

**Reviews:**

*Code round 1:*
- code-reviewer: pass_with_findings, блокеров нет; CR-1 исправлен, CR-2 снят миграцией 005 → [logs/working/task-3/code-reviewer-1.json](logs/working/task-3/code-reviewer-1.json)
- security-auditor: changes_required, 1 major + 2 minor → [logs/working/task-3/security-auditor-1.json](logs/working/task-3/security-auditor-1.json)
- test-reviewer (full): changes_required, 1 major + 2 minor, все исправлены → [logs/working/task-3/test-reviewer-1.json](logs/working/task-3/test-reviewer-1.json)

**Notes carried forward:**
- **Сортировка личности — исправлено миграцией, решение пользователя.** security-auditor нашёл, что `users.id` и все ссылающиеся на него `user_id` наследовали `utf8mb4_uca1400_ai_ci`: регистро-, акценто- и пробелонезависимую сортировку. Воспроизведено живьём на схеме до 005: запрос по `'rogogdbd'` возвращал строку администратора `'RoGogDBD'` вместе с `role='admin'` и `has_access=1`, то есть аккаунт с логином, сворачивающимся к админскому, получал права администратора; двое обычных пользователей, сворачивающихся друг к другу, молча делили одну строку `users` со всеми настройками, чатами и расчётами. Task 1 отмечал эту сортировку, но проверял только «не создаётся ли второй админ» и счёл её безобидной. Вопрос был поднят наверх, **решение принимал пользователь: чинить сейчас**, поскольку прод ещё не выкатывался и это самый дешёвый момент. Итог — `005_identity_collation.up.sql`.
- **Почему `utf8mb4_nopad_bin`, а не `utf8mb4_bin`.** Проверено живьём на `mariadb:11.4`: `utf8mb4_bin` в MariaDB — это PAD SPACE, `'RoGogDBD' = 'RoGogDBD '` под ним всё ещё истина, то есть пробельный вектор остался бы открытым. Только NO PAD закрывает все три вектора сразу. Это отклонение от первоначальной рекомендации, подтверждённое замером, а не выбор по вкусу.
- **Форма guard'а у внешнего ключа.** `ADD CONSTRAINT IF NOT EXISTS ... FOREIGN KEY` — синтаксическая ошибка (ERROR 1064), хотя ровно эта форма работает для `CHECK` в 004. У внешнего ключа `IF NOT EXISTS` ставится после `FOREIGN KEY`. Ошибка была поймана прогоном, а не чтением: первая версия файла сняла все четыре FK и не вернула ни одного. Стоит помнить при следующей миграции с FK.
- **CR-2 снят, а не перенесён.** Расхождение «в памяти ключи побайтовые, в MariaDB — нет» устранено миграцией: после 005 оба хранилища одинаково понимают, что такое «тот же логин». Регрессия закреплена `LoginIdentityIsCaseAndSpaceSensitive`, который идёт по обоим хранилищам и падает на схеме до 005 (проверено на отдельной pre-005 базе).
- **Нетранзакционная пара `SetAccess` + `DecideRequest` — принято и отложено осознанно.** Task 2 передавал этот вопрос «на уровень репозитория». На этом уровне он не решается: обе операции — методы двух разных интерфейсов, зафиксированных блоком Data Models техспека, а их последовательность живёт в `AccessService.SetAccess`, слоем выше. Атомарность требует объединённого метода, то есть правки техспека, а не реализации. Остаточный риск ограничен: `has_access` всегда записывается ровно запрошенным значением, пострадать может только точность `decided_by`/`decided_at` и висящая строка `pending`. Это вопрос точности аудита, а не обхода авторизации. test-reviewer независимо подтвердил, что на уровне репозитория закреплять нечего.
- **TOCTOU в `SetAccess` (CR-1) исправлен.** На нулевой ветке `UPDATE` теперь сверяется постусловие (`has_access == granted`), а не факт существования строки: параллельный `EnsureUser` в этом окне раньше давал администратору подтверждение выдачи доступа, которой не произошло. Ветка повторной попытки намеренно не покрыта автотестом — для входа в неё нужна настоящая гонка двух соединений, — но проверена мутацией и это записано в комментарии у метода.

**Verification:**
- Go на хосте нет; всё выполнялось в `golang:1.25-alpine` с `--network host` и постоянным кэшем модулей. `gofmt -l .`, `go build ./...`, `go vet ./...` — чисто.
- `go test ./... -count=1 -race` с живой MariaDB: `ok internal/repository`, `ok internal/service`. По репозиторию — 21 верхнеуровневый тест, 78 результатов с подтестами, **0 SKIP и 0 FAIL**, 27 подтестов на стороне postgres реально выполнены. Фикстуры после прогона убраны: 0 остаточных строк, обе админские строки нетронуты.
- TDD-порядок настоящий: тесты написаны первыми и подтверждены красными (сборка падала на «`*MemoryRepository` does not implement `service.UserRepository`») до появления реализаций.
- **Мутационные проверки, а не только зелёный прогон.** Возврат поля `Role` в `userModel` роняет все три подтеста регрессии Decision 13 с `ERROR 4025 (23000): CONSTRAINT chk_users_role failed`. Расширение `DoUpdates` в `EnsureUser` до `role`/`has_access` роняет тест на разжалование администратора в обоих хранилищах. Принудительный «строка не задета» в `updateAccessFlag` роняет `SetAccessGrant` с «did not apply» — без сверки постусловия он возвращал бы успех. Все откаты байт-в-байт. Оба ревьюера воспроизвели первые две мутации независимо.
- **Миграция 005 проверена живьём:** до 005 запрос по `'rogogdbd'` отдаёт админскую строку, после — 0 строк, как и по `'ROGOGDBD'` и по `'RoGogDBD '`; похожий логин заводит отдельную строку с `role='user'`, не наследуя доступ. Идемпотентность — файл применён трижды подряд, каждый раз exit 0, число FK и строк не изменилось. Сценарий переезда прода (данные эпохи 001-004, затем 005 отдельно): все счётчики сохранены, `HEX(id)` не изменился, `ON DELETE CASCADE` по-прежнему выносит настройки, чаты, расчёты и заявки, посторонний родитель отвергается 1452. Отдельно подтверждено, что 005 попадает в `//go:embed` и встаёт последним в порядке применения.

---

## Task 4: Личность в сессии и middleware авторизации

**Status:** Done
**Commit:** (см. коммит этой волны)
**Agent:** auth-middleware-writer
**Summary:** `oauth_sessions` начала хранить логин, email и отображаемое имя — они пишутся один раз при входе и переживают ротацию токенов, поэтому «кто вызывает» больше не требует похода в Яндекс на каждый запрос (Decision 1). Появился `server/internal/handler/middleware.go` с `RequireAuth`/`RequireAccess`/`RequireAdmin`/`RequireSameOrigin`: проверка сессии вынесена из `(*AuthHandler).validateSession` в свободную функцию `ResolveSession`, право доступа считает `AccessService` (второй копии Decision 10 в дереве нет), Origin сверяется со списком, а при пустом списке — со `scheme://r.Host`, но не пропускается (Decision 8). Приём готового OAuth-токена (`HandleYandexLogin` + маршрут `POST /auth/yandex`) удалён целиком (Decision 7), `writeAPIDomainError` перестал отдавать текст исходной ошибки во всех восьми ветках (Decision 17), а `main.go` собирает настоящий `AccessService` из репозиториев Task 3 и передаёт его в `NewAuthHandler`, так что строка `users` заводится прямо при входе.

**Deviations:**
- **`AuthHandler` держит интерфейс `SessionStore`, а не `*auth.Store`.** Задача разрешала «метод на `*auth.Store` или отдельную функцию» — взят вариант с узкими интерфейсами (`SessionFinder` для чтения, `SessionStore` для обработчиков входа). Без этого шва handler-тесты требовали бы живой MariaDB, тогда как TDD Anchor требует, чтобы они шли всегда. Сигнатура вызова в `main.go` не изменилась: `*auth.Store` удовлетворяет интерфейсу, `handler.NewAuthHandler(store, cfg, accessService)` — ровно как просила задача.
- **`UpdateSessionTokens` сохранила сигнатуру.** Из двух разрешённых задачей вариантов выбран «UPDATE не трогает колонки личности вовсе»: не упомянутую в `SET` колонку MariaDB не меняет, поэтому личность переживает ротацию без того, чтобы её куда-то передавать — и, значит, без возможности случайно передать пустое значение. Следствие: `HandleRefresh` в этой части не менялся, менять вызывающих не пришлось (edge case задачи про «сверься со всеми вызывающими» отработал вхолостую). Инвариант закреплён отдельным тестом против живой БД и мутацией.
- **`newMux` вынесен из `main()` + добавлен `server/cmd/main_test.go`.** Сверх списка файлов задачи; сделано по major-находке design-ревью: `go build ./...` ловит только одно направление регрессии (обработчик удалён, регистрация осталась → ошибка компиляции), а обратное — обработчик оставлен, а строку регистрации забыли убрать — собиралось бы и проходило все тесты, оставляя `POST /auth/yandex` живым в проде. Теперь состав маршрутов проверяется тестом против настоящей таблицы, а не её копии в тесте. Обёртки уровня сервера (CORS, метрики, ListenAndServe) остались в `main()` — вынесены только маршруты.
- **Вход стал fail-closed в трёх новых точках.** Задача не описывала, что делать при сбое `fetchYandexProfile`, при профиле без логина и при ошибке `EnsureUser`. Все три обрывают вход: сессия не создаётся, куки не выставляются (`502 yandex_profile_failed` и `500 user_ensure_failed`). Логика: сессия без логина неотличима от домиграционной и была бы отвергнута первым же `RequireAuth`, а вход в систему, где строки `users` нет, не даёт ни подать заявку, ни попасть в админский список. Оба новых слага не пересекаются с теми, на которые клиент запускает повтор.
- **`yandexProfile` получил поле `Email` с JSON-тегом `email`.** Побочный эффект: ответ `GET /auth/me` теперь содержит дополнительное поле `email`. Изменение аддитивное, клиент читает только `name`/`login`.
- **`RequireAuth` фильтрует причину отказа по списку известных слагов.** Сверх задачи: резолвер сессии — точка расширения, и `writeError(w, 401, err.Error())` уводил бы клиенту текст любой ошибки, которую вернёт чужая реализация. Незнакомая причина схлопывается в `session_not_found` (повтор на него клиент не запускает), исходная — в лог. Шесть слагов контракта проходят как есть.

**Reviews:**

*Design round 1 (тесты написаны, реализации ещё нет):*
- test-reviewer (design): changes_required — 1 major + 2 minor → [logs/working/task-4/test-reviewer-design-1.json](logs/working/task-4/test-reviewer-design-1.json). Major (нет теста на настоящую таблицу маршрутов `main.go`) закрыт выносом `newMux` и `cmd/main_test.go`. Minor 1 (не покрыт путь «`EnsureUser` прошёл, `CreateSession` упал») закрыт тестом `TestHandleYandexCode_SessionFailureAfterEnsureUser`. Minor 2 (список retry-слагов продублирован из `yandexAuth.js` руками) принят как есть по решению лида; вариант «читать список прямо из JS-файла в Go-тесте» рассматривался и отклонён — он привязал бы тесты сервера к дереву клиента ради риска, который закрывается комментарием со ссылкой на точные строки.

*Code round 1:*
- security-auditor: чисто, 0 находок → [logs/working/task-4/security-auditor-1.json](logs/working/task-4/security-auditor-1.json)
- test-reviewer (full): 1 minor — два теста неуспешного входа проверяли «статус не 204» вместо точных 502 + `yandex_profile_failed`; исправлено, обе мутации (не тот статус, переименованный слаг) теперь ловятся → [logs/working/task-4/test-reviewer-1.json](logs/working/task-4/test-reviewer-1.json)
- code-reviewer: 1 critical + 2 major + 2 minor; critical, оба major и minor 1 исправлены (см. ниже), minor 2 — без действий по договорённости → [logs/working/task-4/code-reviewer-1.json](logs/working/task-4/code-reviewer-1.json)

**Правки по code-round-1 (декомпозиция функций):**
- `HandleYandexCode` (111 строк, шесть задач в одной функции) разобран на оркестратор и шаги: `readLoginRequest` (разбор тела), `establishIdentity` (обмен кода + профиль + `EnsureUser` — у них общий инвариант: пока личность не установлена и не сохранена, ни сессии, ни кук быть не должно), `newLoginSession` (сборка строки сессии), `issueSessionCookies`. Сам обработчик — 34 строки. Отказы шагов возвращаются типом `authFailure` (статус + слаг), ответ пишется в одном месте.
- Оба major закрыты той же правкой, а не отдельной: `HandleRefresh` (72 → 27 строк) получил `sessionForRefresh` (четыре причины отказа) и `rotateSessionTokens`, переиспользует общий `issueSessionCookies` — расхождение атрибутов кук между входом и ротацией теперь структурно невозможно; `fetchYandexProfile` (55 → 33) отдал разбор ответа чистой функции `parseYandexProfile`.
- В затронутых файлах не осталось функций длиннее 50 строк, кроме `main()` (60): это прямолинейная проводка зависимостей без ветвлений, и эта же задача её укоротила (71 → 60, за счёт выноса `newMux`).
- `classifyDomainError` получил nil-guard первой строкой (minor 1): ветки сопоставления по тексту вызывают `err.Error()`, и один невнимательный вызывающий превратил бы ответ об ошибке в панику.
- Декомпозиция дала три новых теста на то, что раньше было проверяемо только через поднятый httptest-сервер: `TestNewLoginSession_MapsIdentityToNullableColumns` (пустое значение → NULL, в сессию кладётся хеш, а не значение куки), `TestParseYandexProfile_NameFallbackChain`, `TestWriteAPIDomainError_NilErrorDoesNotPanic`.
- Рефакторинг поведение не менял: весь набор тестов прошёл до и после без правок самих тестов (кроме трёх добавленных), а мутационный прогон повторён целиком.

**Notes carried forward:**
- **Для Task 5/6 — middleware существует, но в проде ни к чему не подключён.** Задача прямо запрещала трогать сборку цепочек: `RequireAuth`/`RequireAccess`/`RequireAdmin`/`RequireSameOrigin` собраны, покрыты тестами и готовы, но `main.go` их не навешивает. До Task 5/6 `/api/v1/users/` остаётся открытым, а `POST /auth/yandex/code` — без Origin-проверки. Acceptance criteria этой задачи выполнены на уровне middleware; прод-эффект появляется только после навешивания. Это единственное место, где «тесты зелёные» не означает «в проде защищено».
- **Для Task 5/6 — что передавать в `RequireSameOrigin`.** Второй параметр `secure bool` берётся из `cfg.CookieSecure` (признак прод-схемы `https`), первый — из уже существующего `splitCSV(cfg.AllowedOrigins)`. Хардкодить схему или доверять `X-Forwarded-Proto` нельзя: заголовок выставляет тот же, кто присылает `Origin`.
- **Для Task 5/6 — личность берётся через `IdentityFromContext(r.Context())`**, ключ контекста непубличный и типизированный. Владелец данных (`userID`) — это `Identity.Login`; `ContextWithIdentity` экспортирован ради тестов и цепочек.
- **`buildRepositories` расширять больше не нужно** — он уже возвращает пятёрку репозиториев плюс `cleanup`, и `accessService` в `main.go` уже собран. Task 5 переиспользует их, а не пересобирает.
- **`OPTIONS` считается изменяющим методом** — буквально по Decision 8 («на методах, отличных от GET и HEAD»). При непустом `CORS_ALLOWED_ORIGINS` это невидимо: `WithCORS` отвечает на preflight до `mux`. При пустом списке кросс-доменный preflight получит 403 — это и есть заявленное fail-closed поведение, а не регрессия.
- **`writeAPIError` с `err.Error()` остался в трёх местах `http.go`** (ошибки `decodeJSON` и «deepseek не сконфигурирован»). Decision 17 говорит именно про `writeAPIDomainError`; текст ошибки разбора JSON внутренних деталей не содержит. Если Task 6 будет переписывать эти маршруты — стоит заодно свести и их к фиксированным кодам.
- **Слаги 401 теперь сентинелы** (`errAccessCookieMissing` и далее) в одном месте `auth.go` с комментарием о том, что это контракт с `client/src/utils/yandexAuth.js:14-16`. Новый код `session_identity_missing` намеренно вне списка повторов клиента — повтор личность не восстанавливает.
- **Клиентская часть Decision 7 не входила в эту задачу:** implicit-ветка в `client/src/pages/AuthCallback.jsx` и `persistYandexToken` остаются на клиентской задаче. Сервер этот путь уже не принимает — `POST /auth/yandex` отдаёт 404.

**Verification:**
- Go на хосте нет; всё выполнялось в `golang:1.25-alpine` с `--network host` и постоянным кэшем модулей. `gofmt -l .`, `go build ./...`, `go vet ./...` — чисто.
- `go test ./... -count=1 -race` с живой MariaDB (`TEST_DB_DSN` на `poshivon_test`, схема 001-005): все пакеты `ok`, включая `internal/service/costing_test.go` и контрактные тесты Task 3 — регрессий нет. По трём затронутым пакетам (`cmd`, `internal/auth`, `internal/handler`) — 43 верхнеуровневых теста, 119 результатов с подтестами, **0 SKIP и 0 FAIL**. Фикстуры убраны: в `oauth_sessions` после прогона 0 строк.
- TDD-порядок настоящий: все тесты написаны первыми и подтверждены красными (`unknown field YandexLogin in struct literal of type Session`, `undefined: Identity`, `undefined: SessionResolver`, `undefined: SessionStore`, `too many arguments in call to NewAuthHandler`) до появления реализации.
- **Мутационные проверки, а не только зелёный прогон.** Семь мутаций, каждая откачена байт-в-байт, все семь пойманы: (1) `UPDATE` в `UpdateSessionTokens` затирает личность → падает тест ротации против живой БД; (2) `RequireAuth` пропускает сессию без логина → падает `TestRequireAuth_NullIdentitySession`; (3) пустой allowlist трактуется как «пропускать всех» → падает `TestRequireSameOrigin_EmptyAllowlistFallsBackToHost`; (4) из `hasEffectiveAccess` убран `|| role == admin` → падает `TestRequireAccess_AdminBypassesHasAccessFalse` (правило Decision 10 действительно проверяется через сервис, а не переписано в middleware); (5) в 500-ветку `writeAPIDomainError` возвращён `err.Error()` → падает `TestWriteAPIDomainError_NoInternalTextInAnyBranch`; (6) `EnsureUser` убран из `HandleYandexCode` → падает тест на создание пользователя при входе; (7) в `newMux` возвращена регистрация `/auth/yandex` → падает `TestNewMux_LegacyTokenRouteIsGone`. Мутация (7) — ровно то направление регрессии, которое не ловит `go build ./...` и на которое указало design-ревью. По итогам code-round-1 test-reviewer добавлены ещё две: (8) вход при недоступном профиле отвечает 401 вместо 502 и (9) слаг `yandex_profile_failed` переименован — обе теперь падают на `TestHandleYandexCode_ProfileFailureCreatesNoSession`/`_EmptyLoginCreatesNoSession`, до правки эти два теста пропускали любой не-204 статус. После декомпозиции по code-round-1 весь набор мутаций прогнан заново (код переехал, тесты — нет) и дополнен тремя: (10) в сессию кладётся значение refresh-куки вместо её хеша; (11) пустой email пишется пустой строкой вместо NULL; (12) убран nil-guard из `classifyDomainError`. Все двенадцать ловятся.
- Ссылок на `HandleYandexLogin` в коде не осталось (только комментарии, объясняющие удаление); `server/api/openapi.yaml` этот маршрут никогда не документировал, править нечего.

---

## Task 5: Эндпоинты доступа и администрирования, сборка маршрутов

**Status:** Done
**Commit:** (см. коммит этой волны)
**Agent:** access-endpoints-writer
**Summary:** Появился `server/internal/handler/access.go` (`AccessHandler`) с четырьмя маршрутами из таблицы «Контракт API» (`GET /api/v1/access/me`, `POST /api/v1/access/requests`, `GET /api/v1/admin/users`, `POST /api/v1/admin/users/{login}/access`), все ошибки — через `writeAPIDomainError`. Сборка маршрутов и всех middleware-цепочек вынесена в новый `server/internal/handler/routes.go` (`BuildRoutes`) — единственное место, где живёт таблица «Маршруты и их обвязка»: `/api/v1/users/` теперь тоже закрыт `RequireSameOrigin → RequireAuth → RequireAccess`, не только новый контур доступа (US-15/16 для API-слоя). `main.go`'s `newMux` сведён к переводу `cfg` в `RouteDeps` и вызову `handler.BuildRoutes` — той же функции, которую вызывают `access_test.go`/`middleware_test.go`. `CONTACT_EMAIL` добавлен в конфигурацию и отдаётся в `GET /api/v1/access/me`. `normalizePath` (Decision 18) получил третье правило свёртки для `/api/v1/admin/users/{login}/access`.

**Deviations:**
- **`Register(mux)` разделён на `RegisterAccess`/`RegisterAdmin`.** Задача называла один метод по образцу `APIHandler.Register`; разделение сделано, потому что `/api/v1/access/` и `/api/v1/admin/` получают разные цепочки middleware в `routes.go` — один общий `Register` заставил бы `BuildRoutes` либо матчить префиксы вручную внутри общего мультиплексора, либо навешивать на всё одну цепочку. Разделение по методу, а не по разбору пути внутри хендлера, держит решение «какая цепочка» в `routes.go`, а не в `access.go`.
- **`handleListUsers` получил собственную проверку `identity()`.** Изначально — единственный из четырёх хендлеров без неё (Point B code-review, major): право на вызов целиком проверяет `RequireAdmin` в `routes.go`, но это единственный маршрут, отдающий email/роль/доступ каждого пользователя разом, — добавлена перестраховка на случай, если цепочка когда-нибудь окажется собрана неверно. Поведения при верной сборке не меняет (`RequireAuth` всегда отрабатывает раньше), отдельного мутационного теста не заводилось: путь недостижим, пока `routes.go` собран правильно, а его правильность уже покрыта `TestNewMux_ConfigValuesReachMiddleware` и интеграционными тестами `access_test.go`.
- **`isAdminUserLogin` в `metrics.go` привязан к позиции `/api/v1/admin/users/{x}`, а не к любому вхождению `admin/users/` в пути.** Point B security-review (minor): исходная версия схлопывала бы в `:id` и посторонний путь вида `/foo/admin/users/bar`. Чисто вопрос корректности метки Prometheus, не эксплуатируемо, но приведено к буквальному объёму Decision 18.

**Reviews:**

*Design round 1 (тесты написаны, реализации ещё нет):*
- test-reviewer (design): changes_required — 3 major + 2 minor → [logs/working/task-5/test-reviewer-design-1.json](logs/working/task-5/test-reviewer-design-1.json). Major 1 (тест на утечку текста ошибки не доходил до кода хендлера — стаб репозитория падал ещё на собственной проверке доступа админом) закрыт добавлением `getErrByLogin map[string]error`/`listErr`/`setAccessErr` в стаб для точечной инъекции ошибки. Major 2 (400-тела проверялись только по статусу) закрыт — `assertInvalidRequest` теперь проверяет слаг `invalid_request` и отсутствие текста парсера. Major 3 (проводка `cfg → RouteDeps` в `main.go` не проверялась тестом) закрыт `TestNewMux_ConfigValuesReachMiddleware`. Оба minor (нет прямых юнит-тестов `AccessService.ListUsers`; `CONTACT_EMAIL` не покрыт через `config.Load()`) закрыты соответствующими тестами в `service/access_test.go` и новом `config_test.go`.
- **Замечена и задокументирована опечатка в TDD Anchor самой задачи:** прозвучавший в тексте tasks/5.md путь `/api/v1/admin/:id/access` не совпадает с действительным правилом Decision 18 и текущим кодом (`normalizePath`/маршрут `/api/v1/admin/users/{login}/access` — сегмент `users` остаётся). Реализация и тесты написаны по фактическому правилу (с `users`), а не по буквальному тексту анкора; design-ревью независимо подтвердило это чтением tech-spec.

*Code round 1 (после реализации, Point B):*
- test-reviewer (full): 0 critical/major, 1 minor (пустой/пробельный `{login}` в `POST .../access` не покрыт на уровне HTTP-интеграции, только косвенно юнит-тестом сервиса — принято как есть, хендлер не добавляет собственной логики на этом пути) → [logs/working/task-5/test-reviewer-1.json](logs/working/task-5/test-reviewer-1.json). Все три design-major независимо перепроверены мутационным тестированием (сломать защиту → тест падает → откат байт-в-байт) — подтверждены реально закрытыми, не только «по отчёту».
- code-reviewer: 1 major (см. Deviations — `handleListUsers` без `identity()`) + 3 minor → [logs/working/task-5/code-reviewer-1.json](logs/working/task-5/code-reviewer-1.json). Major исправлен. Minor 1 (разделение `Register` не задокументировано) закрыт этой записью. Minor 2 (проверка «identity отсутствует» дублируется между `access.go` и `middleware.go`) — принято как есть, дедупликация ради трёх строк добавляет связность между пакетами без явной выгоды. Minor 3 (`writeError`/`writeAPIError` в `auth.go` дают идентичный вывод при разных декларируемых конвенциях) — вне охвата задачи, оставлено как задача на будущее.
- security-auditor: 1 critical (не дефект этой задачи, см. ниже) + 3 minor → [logs/working/task-5/security-auditor-1.json](logs/working/task-5/security-auditor-1.json). Minor 1 (TOCTOU в `AccessService.SetAccess` между `GetUser` и `DecideRequest`) — уже задокументирован и принят по итогам Task 3, не повторяется здесь. Minor 2 (`RequireSameOrigin`'s same-origin fallback доверяет `r.Host`, что безопасно только пока контейнер приложения не публикует порт напрямую в `docker-compose.prod.yml`) — принято как есть, инвариант инфраструктуры, не кода этой задачи. Minor 3 (привязка `isAdminUserLogin`) исправлена, см. Deviations.

**Критическая находка security-auditor — не дефект Task 5, зона Task 6:** `/api/v1/users/**` (`APIHandler.handleUsers` в `http.go`) уже закрыт `RequireAccess` этой задачей (может вызвать только пользователь с доступом), но сам хендлер по-прежнему берёт `userID` из сегмента URL, а не из сессии, — живой IDOR, подтверждённый auditor'ом на реальной сборке `BuildRoutes` (`ivanov` создал и прочитал чаты `petrov`, подменив логин в пути). Это ровно то, что US-15/Decision 6/tasks/6.md называют явной зоной Task 6 («перенос `/api/v1/users/**` на сессию»); в рамках Task 5 маршруты были только закрыты аутентификацией/правом доступа, а не переписаны на владельца из сессии — то есть находка ожидаема по декомпозиции, но остаётся живой и тривиально эксплуатируемой между закрытием Task 5 и Task 6. **Деплой между Task 5 и Task 6 не рекомендуется.**

**Notes carried forward:**
- **Для Task 6.** `/api/v1/users/**` необходимо переписать на владельца из `IdentityFromContext(r.Context())`, а не из URL — см. критическую находку выше. Пока Task 6 не закрыта, любой аутентифицированный пользователь с доступом может читать/писать чужие чаты, настройки и расчёты.
- **`writeError`/`writeAPIError` в `auth.go`.** Идентичный вывод при разных декларируемых конвенциях (carried forward из Task 4, подтверждено снова code-review). Стоит свести в одну функцию при следующей правке `auth.go`.

**Verification:**
- Go на хосте нет; `golang:1.25-alpine` с `--network host`. `go build ./...`, `go vet ./...` — чисто.
- `go test ./... -count=1` с живой MariaDB (`poshivon_test`): все пакеты `ok`, включая `internal/service/costing_test.go` — регрессий нет. Прогнано трижды подряд после финальных правок (defense-in-depth в `handleListUsers`, привязка `isAdminUserLogin`) — стабильно зелёное.
- Все 16 TDD Anchor тестов из tasks/5.md подтверждены существующими и проверяющими заявленное — независимо перепроверено test-reviewer'ом на Point B, включая мутационные прогоны на трёх самых рискованных исправлениях design-раунда.
- security-auditor независимо прогнал httptest-пробы против настоящей сборки `BuildRoutes` (path traversal, двойные слэши, регистр, закодированные слэши, взаимодействие с CORS/OPTIONS) — обхода цепочек middleware на `/api/v1/admin/`, `/api/v1/users/`, `/api/v1/access/` не найдено; `POST /auth/yandex` подтверждён как 404.

---

## Task 6: Перевод существующих маршрутов на личность из сессии

**Status:** Done
**Commit:** (см. коммит этой волны)
**Agent:** session-ownership-writer
**Summary:** Закрыт живой IDOR, найденный security-auditor'ом на Point B Task 5: `handleUsers` (`server/internal/handler/http.go`) больше не берёт `{userID}` из URL — сегмент убран из всех пяти маршрутов, владелец приходит из `requireIdentity(w, r)` (новый общий хелпер в `middleware.go`, `AccessHandler.identity()` теперь тоже делегирует ему, вторая копия правила «нет личности — 401» из дерева убрана). Индексы оставшихся сегментов пути сдвинуты на один везде, где раньше `{userID}` стоял первым. `server/api/openapi.yaml` обновлён: пять путей `/api/v1/users/...` без `{userID}`, плюс четыре маршрута Task 5 задокументированы.

**Deviations:**
- **`handleUsers` получил собственную проверку `requireIdentity`.** Задача explicitly говорила, что она не нужна — `RequireAccess` уже гарантирует личность в контексте. Добавлена всё равно как перестраховка fail-closed (по образцу `resolveAccessState`): ошибка сборки маршрутов не должна превращаться в работу от имени пустого логина. Чтобы не заводить вторую копию той же проверки, правило вынесено в общий `requireIdentity` в `middleware.go`, и `AccessHandler.identity()` (Task 5) теперь тоже делегирует ему, а не хранит собственную копию.
- **401/403 добавлены в OpenAPI-описания пяти маршрутов `/api/v1/users/*`.** Формально это не входило в «не трогать существующую устарелость» — но эти коды описывают поведение, которое ввела именно эта задача (доступ теперь гейтится сессией), а не унаследованную неточность файла.

**Reviews:**

*Design round 1 (тесты написаны, реализации ещё нет):*
- test-reviewer (design): changes_required — 1 major + 3 minor → [logs/working/task-6/test-reviewer-design-1.json](logs/working/task-6/test-reviewer-design-1.json). Major (тесты на legacy-адреса `TestUsersRoutesAreClosedByAccessChain`/`TestRequireAccess_AdminWithoutFlagPasses` проверяли только статус 200, что после миграции на новые адреса рисковало молча начать принимать 404 как «верно») закрыт — оба теста теперь сидируют чат для владельца сессии и сверяют `user_id`/`id` в ответе. Minor 1 (`openapi.yaml` без тестового покрытия) принят как есть. Minor 2 (один сабтест на удаление чужого чата случайно зелёный ещё до реализации по несвязанной причине) и minor 3 (в `TestHandleUsers_NoAccess_403` не проверялось, что собственное пространство атакующего тоже не тронуто) — оба закрыты, независимо перепроверены на Point B.
- Подтверждено ключевое (finding #1 брифа лида на design-round): тест `TestHandleUsers_ForeignOwnerUnreachable` реально перекрывает все три канала — URL, тело запроса, query-строку, — не только сегмент адреса.

*Code round 1 (после реализации, Point B):*
- test-reviewer (full): 0 critical/major, 1 minor (ветка DeepSeek-интеграции в `handleCalculate`/`handleMarketFeedback` нигде не тестируется с настоящим клиентом — унаследованный пробел, не введён этой задачей) → [logs/working/task-6/test-reviewer-1.json](logs/working/task-6/test-reviewer-1.json). Все 4 design-находки независимо перепроверены чтением кода и воспроизведением мутаций (M3, M4 — лично; M1/M2/M5 — по записи предыдущего прогона), подтверждены закрытыми.
- security-auditor: 0 critical/major, 2 minor → [logs/working/task-6/security-auditor-1.json](logs/working/task-6/security-auditor-1.json). **IDOR подтверждён закрытым независимо**: собственные httptest-пробы против настоящей сборки `BuildRoutes` по всем четырём каналам (сегмент URL, `user_id` в теле, `user_id` в query, чужой `chatID` — включая `restore`, отдельно от набора исполнителя) не нашли обхода; `requireIdentity` не даёт default-allow ни на одном пути отказа. `PostgresRepository.DeleteChat`/`RestoreChat` подтверждены корректно скоуплены по `user_id` на уровне SQL. Minor 1 — контрактный набор `access_repo_test.go` (Task 3) не покрывает владение чатом при `DeleteChat`/`RestoreChat` ни для `MemoryRepository`, ни для `PostgresRepository`: прод-код сейчас верен, но регрессию тут ничего не поймает. Minor 2 — `CostingService.CalculateInChat` не проверяет владение/существование чата явно, спасает только FK-ограничение в БД, поэтому чужой/несуществующий `chatID` отдаёт непрозрачный 500 вместо 404; не эксплуатируемо межпользовательски, вне охвата задачи.
- code-reviewer: 2 major + 1 minor → [logs/working/task-6/code-reviewer-1.json](logs/working/task-6/code-reviewer-1.json). Major 1 — независимо подтверждённый Minor 1 security-auditor'а (тот же пробел в `access_repo_test.go`), см. Notes carried forward. Major 2 (ниже, инцидент лида) исправлен лидом напрямую. Minor (`AccessHandler.identity()` — сквозная функция-обёртка над `requireIdentity`, а `http.go` вызывает `requireIdentity` напрямую — разные соглашения вызова одного правила) принят как есть, чисто косметическое.

**Инцидент при Point B-ревью (действие лида, не исполнителя):** test-reviewer при воспроизведении мутации M4 выполнил `git checkout -- server/internal/handler/http.go` на файле с незакоммиченными изменениями этой задачи — откатил файл до состояния Task 5 (коммит `1fa9574`), потеряв реализацию Task 6. Сам это заметил, восстановил содержимое по собственному более раннему полному чтению файла и сверил объём диффа (`+39/-17`, 56 строк) с ожидаемым. Реконструкция оказалась неполной: в `buildMarketFeedbackInputFromCalculation` (функция, вообще не относящаяся к этой задаче) потерялась строка `MarketStatus: result.MarketStatus,` — её отсутствие поймал code-reviewer как major-находку («скрытая регрессия»). Лид сверил текущий файл построчно с `git diff 1fa9574`, подтвердил, что это единственное расхождение вне ожидаемых правок `handleUsers`, вернул строку и перепроверил полную сборку/тесты — диф `http.go` после этого чист. **Урок:** `git checkout --` небезопасен для отката мутации на файле с уже имеющимися незакоммиченными правками — откатывает не мутацию, а весь файл до последнего коммита; safer — точечный `git diff`/ручной revert конкретной правки, либо `git stash`/копия файла перед мутацией.

**Notes carried forward:**
- **Для будущей задачи (не Task 3, она уже закрыта, и не эта).** `access_repo_test.go` не покрывает владение чатом при `DeleteChat`/`RestoreChat` ни для `MemoryRepository`, ни для `PostgresRepository` — независимо отмечено и code-reviewer, и security-auditor. Прод-код сейчас корректен (SQL уже скоупит по `user_id`, подтверждено чтением и живым тестом), но без контрактного теста будущая регрессия в этом месте останется незамеченной ровно тем же классом уязвимости, который закрывает эта задача. Стоит завести отдельной строкой в бэклог укрепления, не блокирует деплой.
- `CostingService.CalculateInChat` отдаёт 500 вместо 404 на чужой/несуществующий `chatID`, спасает только FK — не эксплуатируемо, но нечистый contract; чинить вместе с любой будущей правкой этого метода, не отдельной задачей ради этого одного.
- Клиентская часть (`client/src/utils/panelApi.js`) всё ещё строит legacy URL с `{userID}` — задачи 7/8/10/11 её меняют, эта задача клиента не касалась.

**Verification:**
- Go на хосте нет; `golang:1.25-alpine` с `--network host`. `go build ./...`, `go vet ./...` — чисто.
- `go test ./... -count=1` с живой MariaDB (`poshivon_test`): все пакеты `ok` и после лидовской правки `MarketStatus`, прогнано дважды подряд — стабильно зелёное.
- Все 5 TDD Anchor тестов подтверждены существующими и проверяющими заявленное. Пять мутаций (полный откат владения; `user_id` в query переопределяет владельца; чужой владелец в `DeleteChat`; терпимость к legacy-сегменту; опечатка сдвига индекса в `restore`) — каждая поймана соответствующим тестом, откат подтверждён `git diff` без остатка.
- security-auditor независимо воспроизвёл обход по всем четырём каналам (URL/тело/query/чужой `chatID` на `restore`) — обхода не найдено; живой смоук на отдельном порту (:18080) с настоящей MariaDB подтвердил: неавторизованному пользователю без доступа больше не видны чужие данные, `GET /api/v1/users/RoGogDBD/chats` от имени другого авторизованного пользователя даёт 404 вместо чужих чатов (до фикса возвращал их).

---

## Task 7: Клиентский гейт доступа и правки авторизации

**Status:** Done
**Commit:** (см. коммит этой волны)
**Agent:** client-access-gate-writer
**Summary:** Панель (`Panel.jsx`) теперь запрашивает `GET /api/v1/access/me` третьим вызовом bootstrap-эффекта и получает третье состояние машины `status` — `"no-access"`, при котором рендерится новая `AccessRequestBanner` (контактный email, кнопка «Запросить доступ», 409 трактуется как «уже на рассмотрении», не ошибка) вместо рабочего интерфейса; оба эффекта загрузки данных (`loadSettings`/`loadChats`, история расчётов) теперь не срабатывают, пока `status !== "ready"` — до этой задачи они триггерились по `userID`, известному раньше результата проверки доступа. `panelApi.js` лишился `userID` во всех девяти экспортах и путях (под контракт Task 6). `buildYandexAuthUrl` добавляет `scope` и случайный `state` (Decision 9/12), `AuthCallback.jsx` сверяет `state` перед обменом кода и снимает его из `sessionStorage` одноразово; implicit-приём токена (`persistYandexToken`, чтение `access_token` из хэша) удалён целиком (Decision 7).

**Deviations:**
- **`saveAuthState` не экспортируется**, в отличие от `saveAuthReturnTo`: единственный законный источник `state` — `buildYandexAuthUrl`, внешняя запись означала бы рассинхрон с реально отправленным адресом. Подтверждено code-review как обоснованное решение.
- **`createAuthState` — запасной путь через `getRandomValues`** позади `randomUUID`: последний доступен только в secure context, а dev-сервер слушает `0.0.0.0` и открывается по IP без https. Оба пути дают 128 бит энтропии, подтверждено security-auditor'ом отдельным прочтением кода генерации и сравнения.
- **Кнопка плашки переиспользует класс `.panel__theme-toggle`**, а не заводит новый: задача называла только контейнерные классы (`.panel__card`/`.panel__notice`/`.panel__empty`), непойстилженная кнопка выглядела бы сломанной. Code-review подтвердил: это уже де-факто основной класс кнопки-пилюли в проекте (тот же, что у «Рассчитать»), не разовый хак. Полировка стиля — Task 17 (Фаза 2).

**Reviews:**

*Без design-round:* автотестов в клиенте нет (`client/package.json` — только `dev`/`build`/`preview`), задача сама фиксирует это в TDD Anchor и не включает test-reviewer в реестр ревьюеров.

*Code round 1 (после реализации, Point B):*
- security-auditor: **0 находок**, статус `clean` → [logs/working/task-7/security-auditor-1.json](logs/working/task-7/security-auditor-1.json). Независимо перепроверена защита `state`/CSRF: путь генерации (оба варианта дают минимум 128 бит энтропии, ни один не может тихо выродиться в пустое значение), путь сравнения (`consumeAuthState()` возвращает `null`, никогда `undefined` — отсутствующий сохранённый `state` гарантированно считается несовпадением, сценария `undefined === undefined` нет), одноразовость (state снимается атомарно до сравнения — очищен при совпадении, несовпадении и любом исключении внутри обмена кода одинаково). Implicit-приём токена подтверждён полностью удалённым (grep по всему дереву клиента — только объясняющий комментарий). Клиентский `hasPanelAccess` подтверждён используемым только для выбора рендера, не как источник авторизации ни для одного запроса.
- code-reviewer: 0 critical/major, 5 minor → [logs/working/task-7/code-reviewer-1.json](logs/working/task-7/code-reviewer-1.json). Подтверждена корректность машины состояний `status`, обеих охран эффектов загрузки данных, полноты удаления `userID` из `panelApi.js`, соответствия 409-контракта между `accessApi.js` и `AccessRequestBanner.jsx` и с фактическим поведением сервера (`access.go:89-91`). Minor 1 (React StrictMode в dev дважды монтирует эффект `AuthCallback`, второй проход находит `state` уже снятым первым и показывает ложную «Ошибка авторизации» перед редиректом — коллизия ровно с тем ручным шагом проверки, который прописан в самой задаче) — **исправлено лидом**: добавлен `useRef`-guard (`hasRun`), переживающий двойное монтирование StrictMode на одном экземпляре компонента, тело эффекта теперь гарантированно выполняется один раз за настоящее монтирование. Minor 2 (нет проверки на отсутствие `window.crypto` целиком) — принято как есть: `window.crypto` доступен во всех браузерах, которые поддерживает текущий Vite-таргет, добавлять защиту от практически недостижимого сценария — не то, ради чего стоит усложнять код. Minor 3 (дублирование `request()`-логики между `accessApi.js`/`panelApi.js`, чуть разошлись — 405 обрабатывается только в `panelApi.js`) и minor 5 (унаследованный проглоченный `catch` в `handleLogout`, не введён этой задачей) — приняты как есть, вынесены в Notes carried forward. Minor 4 (клиентское `|| role === "admin"` в `hasPanelAccess` — no-op, поскольку сервер уже сворачивает роль в `has_access`) — не баг, а буквальное зеркалирование серверного условия по прямому требованию задачи (Decision 10); оставлено без изменений.

**Notes carried forward:**
- **Для будущей задачи (не блокирует).** `accessApi.js` и `panelApi.js` держат почти идентичную обёртку `request()`/обработку ошибок раздельно, и они уже разошлись (405 есть только в одном месте) — стоит когда-нибудь свести в общий модуль, не разовой задачей ради этого одного.
- `Panel.jsx`'s `handleLogout` глотает ошибку сети в пустой `.catch(() => {})` — унаследовано, не введено этой задачей, не трогалось.
- Ручная верификация (5 шагов из Verification Steps → User в tasks/7.md: плашка без доступа, кнопка запроса и 409, повторный вход, старый implicit-URL, подмена `state`) **не выполнена** — агент без браузера, лид тоже не запускал живой Vite-сервер в этой сессии. Требует человека или Playwright MCP до деплоя; формально относится к Pre-deploy QA (волна 11), но стоит держать в уме раньше.

**Verification:**
- `cd client && npm install && VITE_YA_CLIENT_ID=testclient npm run build` — чисто, до и после правки StrictMode-guard'а лидом. `VITE_YA_CLIENT_ID` обязателен при сборке: без него `buildYandexAuthUrl` рано выходит, и весь остальной код функции (включая `scope`/`state`) вычищается как мёртвый Vite'ом — не баг, особенность статической замены `import.meta.env`. Собранный бандл вручную проверен на присутствие `scope`, `state`, `randomUUID`, `getRandomValues` — все на месте.
- Автотестов нет (см. Reviews). `go build ./...` для серверной части не запускался этой задачей — файлы сервера не менялись.

---

## Task 8: Раздел «Пользователи» для администратора

**Status:** Done
**Commit:** (см. коммит этой волны)
**Agent:** admin-users-section-writer
**Summary:** Последняя клиентская задача Фазы 1. Новый `AdminUsersSection.jsx` тянет `GET /api/v1/admin/users` при монтировании и рендерит по строке на пользователя с чекбоксом `has_access`; админские строки (по полю `role` самой строки, не по сравнению с логином сессии — обе админские строки, не только своя) рендерятся `checked disabled` вовсе без `onChange` — единственная защита от отправки запроса на снятие доступа у админа, поскольку сервер такой вызов не отклоняет (`RequireAdmin` смотрит только на роль вызывающего, Decision 10 лишь делает случайный успех такого вызова безвредным). `Panel.jsx` получил третий пункт навигации «Пользователи» (виден только при `access?.role === 'admin'`), бинарный тернарник секций стал трёхветочным (`settings`/`users`/`workspace` по умолчанию), блок `panel-summary` перенесён внутрь ветки `workspace` — раньше рендерился поверх всех секций, включая `settings`.

**Deviations:**
- **`isAdmin` дополнительно охраняет саму ветку рендера `"users"`**, не только кнопку навигации — устаревшее значение `activeSection === "users"` не может отрендерить раздел администратора не-админу, падает в `workspace`. Задача явно требовала охраны только кнопки; расширение подтверждено code-review как разумная подстраховка, не избыточность.
- **Локальное состояние строки после тоггла обновляется из отправленного `granted`, не из тела ответа** — `POST /api/v1/admin/users/{login}/access` отдаёт `204 No Content` (подтверждено чтением `access.go:162`), тела для чтения нет; успех дожидается разрешения промиса, переключение остаётся неоптимистичным (состояние меняется только после ответа, при ошибке — остаётся прежним).
- **`disabled` без `readOnly` на чекбоксе админской строки** — подтверждено чтением `react-dom.development.js`: `disabled` сам по себе снимает предупреждение React о controlled input без `onChange`, второй атрибут не нужен.

**Reviews:**

*Без design-round:* автотестов в клиенте нет (см. Task 7), задача сама это фиксирует.

*Code round 1 (после реализации, Point B):*
- security-auditor: 0 critical/major, 1 minor → [logs/working/task-8/security-auditor-1.json](logs/working/task-8/security-auditor-1.json). **Гарантия «для админской строки запрос не может уйти вовсе» подтверждена независимо**: прочитаны `access.go`/`access.go` (service)/`middleware.go` — подтверждено, что `SetAccess` действительно не проверяет роль цели, `RequireAdmin` проверяет только вызывающего; в `AdminUsersSection.jsx` админская ветка рендерит структурно другой `<input>` без `onChange` вовсе, не просто `disabled`-версию с обработчиком; `isAdminRow` берётся из поля `role` самой строки; `login` в `setUserAccess` всегда из серверных данных строки, никогда не из пользовательского ввода. Minor (нет unmount-guard в `handleToggle`, в отличие от эффекта загрузки) — исправлен лидом вместе с находками code-review, см. ниже.
- code-reviewer: 1 «critical» (по формальному правилу длины функции — 121 строка, из них большая часть JSX, сам ревьюер отметил, что реальная сложность ниже, чем ярлык подразумевает) + 2 major → [logs/working/task-8/code-reviewer-1.json](logs/working/task-8/code-reviewer-1.json). **Все три исправлены лидом**: (1) гонка при параллельном тоггле нескольких строк — `pendingLogin` был одиночным значением, вторая параллельная строка снимала блокировку с первой; заменён на `Set` логинов; (2) отсутствие unmount-guard в `handleToggle` (в отличие от эффекта загрузки, который его уже имел) — добавлен `isMounted`-ref, разделяемый обоими путями; (3) длина функции — разметка строки вынесена в `UserRow`, вся стейт-логика — в хук `useAdminUsers`, сам `AdminUsersSection` сведён к 33 строкам. Все четыре самостоятельно заявленных отклонения подтверждены обоснованными (охрана `isAdmin` на ветке рендера; обновление состояния из отправленного `granted`, не из тела 204-ответа — сверено с `access.go:162`; `disabled` без `readOnly` — сверено с исходником `react-dom`; `git diff -w` на `Panel.jsx` подтвердил, что реальный диф — это ровно `isAdmin` + третья кнопка + трёхветочный рендер + перенос `panel-summary`, ничего не потеряно в переформатировании).

**Notes carried forward:**
- Ручная верификация (3 шага из Verification Steps → User в tasks/8.md: список видим админу с чекбоксами, тоггл персистится после перезагрузки, раздел не виден обычному пользователю) **не выполнена** — как и для Task 7, требует человека или Playwright MCP, относится к Pre-deploy QA (волна 11).
- Phase 1 клиента полностью завершена этой задачей: `AccessRequestBanner` (Task 7) + `AdminUsersSection` (эта задача) — обе стороны контура доступа на клиенте на месте. Полировка стиля обеих — Task 17, Фаза 2.

**Verification:**
- `cd client && VITE_YA_CLIENT_ID=testclient npm run build` — чисто, и после правок лида (`UserRow`, `useAdminUsers`, `Set` вместо строки, `isMounted`-ref).
- Автотестов нет. Серверные файлы не менялись.

---

## Task 9: Конфигурация деплоя, проксирование и прогон тестов в CI

**Status:** Done
**Commit:** (см. коммит этой волны)
**Agent:** deploy-config-writer
**Summary:** Только конфигурация CI/CD и инфраструктуры, без правок прикладного кода. `CONTACT_EMAIL` (Task 5) проведён через все пять хопов до контейнера (`env:`/`envs:`-разрешённый список у `appleboy/ssh-action`/`.env`-heredoc/`docker-compose.prod.yml`/`docker-compose.yml` для dev-паритета). `/auth/refresh` теперь проксируется в обоих nginx-конфигах (heredoc в `deploy.yml` и `client/nginx.conf`), мёртвая запись `/auth/yandex` (маршрут удалён на сервере Task 4) убрана из обоих. Новый `.github/workflows/test.yml` — переиспользуемый workflow (`workflow_call` + прямой `push`/`pull_request`) с сервис-контейнером `mariadb:11.4` и `go test ./... -v`; `deploy.yml` получил `jobs.test: uses: ./.github/workflows/test.yml` и `needs: test` на существующей задаче — красные тесты теперь блокируют деплой.

**Deviations:**
- **Добавлен шаг накатки миграций в `test.yml`, которого не было в задаче.** Сервис-контейнер GHA поднимается с пустой БД, а `migrations.Run` выполняется только при реальном старте приложения — этого в CI никогда не происходит. Без шага контрактные тесты падали бы (нет таблиц), а не пропускались. Цикл по `server/migrations/*.up.sql` через `docker exec` в сервис-контейнер повторяет `glob+sort` из `migrate.go` — подтверждено чтением `migrate.go` и deploy-reviewer'ом независимым прогоном той же логики на живой MariaDB (порядок 001→005 совпал, `0001_init.sql`, PostgreSQL-only файл, корректно исключён из обоих glob'ов по той же причине). `docker exec`, а не прямой `mysql`-клиент с раннера (порт уже проброшен на `127.0.0.1:3306`) — выбрано потому, что наличие `mysql`/`mariadb` CLI на `ubuntu-latest` не гарантировано, тогда как образ сервис-контейнера всегда его содержит; deploy-reviewer отметил это стилистическим minor (GitHub документирует `mysql`-клиент как предустановленный на `ubuntu-latest`, так что путь через раннер был бы чуть идиоматичнее), не блокирующим.
- **`.env`-heredoc: `CONTACT_EMAIL=${CONTACT_EMAIL:-}` вместо буквального `${CONTACT_EMAIL}` из текста задачи.** Заявленное на этапе реализации обоснование («несуществующий GitHub-секрет даст `unbound variable` под `set -euo pipefail`») **проверено и отклонено двумя независимыми ревьюерами** (security-auditor и code-reviewer, одинаковый вывод): ссылка на несуществующий секрет в `env:`-блоке GitHub Actions разворачивается в пустую строку, а не в отсутствующее присваивание, поэтому `set -u` на голом `${CONTACT_EMAIL}` в этом сценарии не сработала бы. Сама правка осталась — она безвредна и корректно отражает то, что `config.go` называет легитимной конфигурацией (пустой `ContactEmail` — штатное поведение, не ошибка), — но зафиксированное здесь обоснование исправлено на верное: `:-` — не защита от аварийного завершения деплоя, а просто явное, самодокументирующее выражение того же результата, который и так получился бы.
- **`test.yml`'s `push:` ограничен `branches: ["**"]`**, не голый `on: [push, pull_request]` — чтобы тегированный пуш не запускал прогон дважды (`'**'` не матчит теги, второй прогон и так придёт через `workflow_call` из `deploy.yml`). Подтверждено обоими ревьюерами как корректная семантика GHA.

**Reviews:**

*Без design-round:* задача инфраструктурная (skill `deploy-pipeline`), design-review тестов неприменим — код приложения не менялся.

*Code round 1 (после реализации, Point B):*
- security-auditor: 0 critical/major, 2 minor → [logs/working/task-9/security-auditor-1.json](logs/working/task-9/security-auditor-1.json). CI-креды (`ci_root`/`poshivon`/`poshivon`) подтверждены полностью изолированными от прод-секретов (разное пространство имён переменных, сверено с `docker-compose.prod.yml`), `envs:`-список конгруэнтен `env:`-блоку, новый `location = /auth/refresh` повторяет заголовочную гигиену соседей, `RequireSameOrigin` (Decision 8) не полагается на `X-Forwarded-Proto`, так что его отсутствие/наличие на этом блоке значения не имеет. Оба minor — унаследованные/несущественные условия (сторонние Actions закреплены за мутable-тегами, а не SHA; root-пароль CI не маскируется логом GHA, т.к. не зарегистрирован как секрет — риск нулевой ввиду одноразовости контейнера).
- deploy-reviewer: `approved`, 0 critical/major, 5 minor → [logs/working/task-9/deploy-reviewer-1.json](logs/working/task-9/deploy-reviewer-1.json). Независимо перепроверил все три закрываемых разрыва живым прогоном (не поверил отчёту): пять хопов `CONTACT_EMAIL` сверены байт в байт, оба nginx-конфига прошли `nginx -t`, шаг миграций воспроизведён на реальной MariaDB — порядок совпал, admin-count = 2, `go test ./... -v` — 384/384, 0 skip. Единственное содержательное замечание (`docker exec` vs прямой `mysql`-клиент) — стилистическое, не блокирует.
- code-reviewer: 0 critical/major, 3 minor → [logs/working/task-9/code-reviewer-1.json](logs/working/task-9/code-reviewer-1.json). Независимо подтвердил конгруэнтность `env:`/`envs:` (28/28), валидность `${{ job.services.mariadb.id }}` (сверено с документацией GitHub через веб-поиск), точное соответствие шага миграций `migrate.go`, отсутствие правок `config.go` (сверено дифом — `CONTACT_EMAIL` пришёл из Task 5, не задублирован здесь). Тот же вывод про ошибочное обоснование `:-`-фолбэка, что и security-auditor (см. Deviations выше). **Два minor исправлены лидом**: отсутствие `concurrency`-группы в `test.yml` (push и pull_request на одной ветке PR запускали прогон дважды) — добавлена группа, ключ включает `github.event_name`, чтобы деплойный прогон через `workflow_call` в дедупликацию не попадал и не отменялся; повтор литерала `ci_root` трижды в файле (`services.<id>.options` не видит контекст `env` — ограничение самого GHA, не недосмотр) — добавлен поясняющий комментарий. Третий minor (bookkeeping `schema_migrations` не заполняется шагом миграций) принят как есть — ни один тест на это не полагается.

**Notes carried forward:**
- **Требуется человек до следующего тегированного деплоя:** создать секрет репозитория `CONTACT_EMAIL` в GitHub — эта задача не может завести секрет сама; без него плашка молча останется без контактного email, без единой ошибки где-либо в пайплайне.
- **Требуется человек / живой прогон:** запушить ветку или открыть PR и убедиться в Actions, что `test.yml` реально стартует через `uses: ./.github/workflows/test.yml`, сервис-контейнер MariaDB проходит `--health-cmd`, шаг миграций отрабатывает на настоящем hosted runner'е (не только локально симулированном), и что намеренно сломанный тест блокирует `build-and-deploy`. Пост-деплойный смоук: `docker exec poshivon_app env | grep CONTACT_EMAIL`, `curl -i -X POST https://<host>/auth/refresh` (JSON, не `text/html`), `SELECT COUNT(*) FROM users WHERE role='admin'` = 2 на реальной прод-БД.
- Стилистическая рекомендация deploy-reviewer'а (прямой `mysql`-клиент с раннера вместо `docker exec` в сервис-контейнер) не применена — обоснование выбора `docker exec` (гарантия наличия клиента) сочтено достаточным, не стоит churn ради стиля.

**Verification:**
- Все затронутые YAML (`test.yml`, `deploy.yml`, `docker-compose.yml`, `docker-compose.prod.yml`) — `yaml.safe_load` чисто, включая после правок лида.
- `go test ./...` (полный набор, живая MariaDB, модульный кэш `poshivon_gomodcache` — сеть до `proxy.golang.org` была нестабильна в момент проверки, кэш обошёл проблему) — все пакеты `ok`.
- deploy-reviewer независимо воспроизвёл весь пайплайн миграций на свежем `mariadb:11.4`: 001→005 в правильном порядке, admin-count 2, 384/384 тестов, 0 skip/fail — то же самое, что заявил исполнитель, но перепроверено, а не принято на веру.
- Оба nginx-конфига (heredoc `deploy.yml`, `client/nginx.conf`) прошли `nginx -t` дважды — при реализации и повторно при ревью.

---

## Task 12: Test Audit

**Status:** Done
**Agent:** test-audit (аудитная задача, отдельного исполнителя-ревьюера нет — сам аудит и есть ревью)
**Summary:** Сквозной аудит тестового покрытия Phase 1 (Tasks 1-9) против раздела Testing Strategy tech-spec.md. **Вердикт: PASS**, 5 minor находок, ни одна не требует отдельной задачи на исправление. Полный отчёт: [logs/working/task-12/test-audit-report.md](logs/working/task-12/test-audit-report.md).

Все пять «несущих» проверок, которые задача требовала разобрать явно и с максимальным вниманием, подтверждены закрытыми по-настоящему, не только по названию теста:
- **Admin-demotion regression (Decision 11)** — `EnsureUser` на существующей `role='admin'`/`has_access=true` строке не сбрасывает оба поля при повторном логине; проверено чтением после записи, а не отсутствием ошибки. Покрытие шире, чем требовал tech-spec: тот же инвариант дополнительно проверен через `UpsertSettings`/`CreateChat`/`AppendCalculation`.
- **`SetAccess(granted=false)` на реальной БД** — подтверждено, что тест не просто существует, а реально выполнился против `PostgresRepository` (`TEST_DB_DSN` был выставлен, `--- PASS: .../postgres` есть в логе, `grep SKIP` по verbose-логу пуст по всем 384 кейсам) — закрывает ровно тот сценарий, где Go zero-value баг мог бы тихо «работать» на map-хранилище и молча не писать `false` через SQL-слой.
- **`CreateRequest` — `RowsAffected`-семантика** — все три ветки (первая заявка/дубликат `pending`/повтор после `rejected`) проверены по фактическому счётчику, не по коду ошибки; `decided_by`/`decided_at` сверяются со значениями, снятыми до повтора, а не с литералами.
- **Матрица Origin-проверки (Decision 8)** — все пять веток покрыты, включая именно ту (Origin == r.Host при пустом allowlist → проходит), которая при пересмотре тех-спека меняла направление; каждая отклоняющая ветка дополнительно проверяет, что флаг доступа не изменился. Login-CSRF вариант на `/auth/yandex/code` тоже покрыт — ноль обменов токена с фейковым Яндексом при несовпадении `state`.
- **Отсутствие утечек текста ошибки** — все восемь веток `writeAPIDomainError` (включая унаследованные 400/404/429/500/503/504, не только новые 403/409 этой фичи) проверены одинаково тщательно.

Оба намеренных исключения (клиентских тестов нет — подтверждено отсутствием тест-раннера и файлов; `notifier_test.go` — Phase 2/Task 16, ещё не существует) подтверждены корректно проигнорированными, не пропущенным покрытием.

**5 minor находок** (ни одна не блокирует, follow-up не требуется): тавтологичный кейс на пустой `CONTACT_EMAIL` (фикстура сама подставляет дефолт, реальное пустое значение до хендлера не доходит); один тест на удалённый `/auth/yandex`, который физически не может упасть независимо от `routes.go` (реальное покрытие уже есть в другом месте через `BuildRoutes`); тест `RowsAffected` обращается к SQL-константе напрямую, а не через сам `CreateRequest` (смягчено — путь ошибки всё равно покрыт контрактным тестом); один assertion сверяет дефолтное значение настроек, а не маркер владельца (реальная проверка владения есть в соседнем тесте); нет отдельного 401-кейса на `POST /api/v1/access/requests` без сессии (покрыт транзитивно общим префиксом, не пункт из Testing Strategy).

`go test ./...`: 384/384, 0 fail, 0 skip — прогнано как подтверждающее свидетельство, не как сам аудит.

---

## Task 10: Code Audit

**Status:** Done
**Agent:** code-audit (аудитная задача, отдельного исполнителя-ревьюера нет)
**Summary:** Сквозной архитектурный аудит Phase 1 (Tasks 1-9) как единой системы, а не девяти изолированных диффов. **4 critical, 8 major, 9 minor.** Полный отчёт: [logs/working/task-10/code-audit.md](logs/working/task-10/code-audit.md). Все явно запрошенные инварианты (единственность shared-ресурсов в `main.go`, соответствие middleware-цепочек таблице Architecture, инвариант `userModel` Decision 13, компайл-тайм проверки интерфейсов, отсутствие `userID` в URL) — чисто.

**Critical находки** (см. отчёт за полным разбором и предложенными фиксами):
- **C1 — `RequireSameOrigin`'s same-origin fallback несовместим с обоими прокси проекта.** Vite dev-прокси (`changeOrigin: true`) и nginx (`$host` обрезает порт) оба меняют `Host`, который видит Go, так что сравнение `Origin == scheme://r.Host` не проходит — **любой мутирующий запрос, включая сам логин, получает 403** и в `npm run dev`, и в docker-compose dev-стеке. На проде работает только по совпадению (порт 443 неявный) и только если секрет `COOKIE_SECURE` действительно выставлен (дефолт `false`, без валидации). Перепроверено лидом лично чтением `middleware.go`/`vite.config.js`/`client/nginx.conf`/`docker-compose.yml` — подтверждено.
- **C2 — Decision 17 нарушен в четырёх местах `http.go` (строки 99, 124, 167, 219).** `writeAPIError(w, 400, err.Error())` отдаёт сырой текст JSON-парсера (включая имена внутренних Go-типов) без единой строки в лог. Task 4 явно отложил это на Task 6 (`decisions.md:152`), Task 6 переписал ровно эти хендлеры и не подхватил; Task 5 независимо пришла к противоположному, верному решению для того же класса ошибки в `access.go` — два противоречащих соглашения в одном пакете.
- **C3 — `generateToken()` (`auth.go:702`) проглатывает ошибку `crypto/rand`.** При сбое возвращает 64 нуля вместо секрета сессии; `costing.go:790`'s `newChatID` проверяет идентичный вызов, `auth.go` — нет.
- **C4 — `Panel` — функция на ~955 строк** внутри файла на 1403 строки (порог критичности — 100 строк). Task 7 и Task 8 обе добавляли в один и тот же монолит, хотя обе корректно выносили *свои* новые куски (`AdminUsersSection.jsx`, `AccessRequestBanner.jsx`) — форма настроек и рабочая область остались инлайн.

**Major находки (кратко):** `GET /auth/me` в bootstrap-эффекте панели возвращает живой вызов к Яндексу на каждой загрузке (противоречит Decision 1), а сбой Яндекса тихо разлогинивает пользователя без единой строки в консоли; `normalizePath` не схлопывает `chatID` (24-hex, без дефиса) — Task 6 переместила его в тот же незащищённый индекс, который Decision 18 считала закрытым, неограниченный рост меток Prometheus; `MemoryRepository` принимает любую строку статуса заявки, где Postgres проверяет CHECK-констрейнтом — опечатка проходит контрактные тесты на memory-сторе и падает только на MariaDB; dev `docker-compose.yml` не задаёт `APP_STORAGE`, весь фичер работает на `MemoryRepository`, хотя миграции накатываются на реальную MariaDB — засеянные админы невидимы; `GetRequest` — мёртвый метод интерфейса, отмеченный ещё в отчёте Task 3 и так и не решённый; CORS объявляет только `GET, POST, OPTIONS`, хотя клиент шлёт `DELETE` — та самая посылка, из которой Decision 16 выбрала POST вместо PATCH, уже неверна в смёрженном коде.

**Отчёт заканчивается таблицей cross-task drift** — сопоставлением того, что каждая задача утверждала в своём отчёте, с тем, что реально в коде. Она прямо показывает: большинство critical/major находок — это ровно то, что отдельные ревью каждой задачи в принципе не могли увидеть, потому что каждая видела только свой дифф.

---

## Task 11: Security Audit

**Status:** Done
**Agent:** security-audit (аудитная задача, отдельного исполнителя-ревьюера нет)
**Summary:** Сквозной аудит безопасности Phase 1 против всех 11 ключевых Decision (1, 2, 6, 7, 8, 9, 10, 11, 13, 17, 18), с независимой верификацией: httptest-пробы против настоящей сборки `BuildRoutes`, живой прогон миграций 001-005 на MariaDB со сталкивающейся строкой, `govulncheck`, `npm audit`. **14 находок: 1 critical, 1 high, 6 medium, 6 low.** Полный отчёт: [logs/working/task-11/security-auditor-1.json](logs/working/task-11/security-auditor-1.json).

**9 из 11 Decision — implemented-as-specified** (с evidence по каждой, включая обе живые находки предыдущих волн — IDOR на `/api/v1/users/**` и identity-коллизия Decision 19 — подтверждены закрытыми, без регрессии). **2 расходятся с кодом**: Decision 17 (тот же C2 из Task 10, независимо подтверждено) и Decision 18 (нормализация метрик обходится закодированным слэшем, а базовая проблема неограниченной кардинальности покрыта только для одного маршрута — см. SEC-11-02, high).

**SEC-11-01 (critical) — самая серьёзная находка всей волны аудита, живо воспроизведена на MariaDB.** Миграция 004 сеет двух администраторов `ON DUPLICATE KEY UPDATE role='admin'` **под старой коллацией** `utf8mb4_uca1400_ai_ci` (регистро/акцент/пробело-нечувствительной) — коллацию на `utf8mb4_nopad_bin` меняет только миграция 005, которая идёт строго после и не переписывает уже применённую 004 (`migrate.go` применяет каждый файл ровно один раз). Если в `users` до деплоя этой фичи уже существовала строка, чей логин совпадает с `RoGogDBD`/`irina2000aleshina` с точностью до регистра/акцента/пробела — а `upsertUser` был анонимно достижим для *любого* вызывающего через дореформенный `/api/v1/users/{userID}/settings` (та самая посылка Decision 6 для закрытия этого пути) — эта чужая строка получает `role='admin'` навсегда. После 005 сравнения становятся байт-точными, реальный админ логинится и **получает свежую строку с `role='user'`**, а не свою старую. Воспроизведено вживую: посеяна строка-коллизия `rogogdbd`, применены реальные файлы 004→005 → строка `rogogdbd` осталась ADMIN, реальный логин `RoGogDBD` после входа получил `role='user'`. Продакшн ещё не деплоился с этой фичей — находка ловится сейчас бесплатно и станет практически необратимой после первого деплоя. **Требует решения пользователя, см. ниже.**

**Прочие находки (medium/low):** нет аудиторской записи о выдаче/снятии доступа, когда у цели нет строки заявки (нельзя восстановить, кто и когда выдал доступ после инцидента); достижимый DoS-advisory в `golang.org/x/text` (`govulncheck`, symbol-level); 6 npm-advisory в сборочной цепочке клиента (в т.ч. 4 high); нет security-заголовков ни на одном из двух nginx-слоёв — при `COOKIE_SAMESITE=none` (единственный секрет без валидации) чекбокс выдачи доступа становится кликджекинг-целью; `COOKIE_SECURE` (тот же корень, что и C1 из Task 10) незаметно решает, может ли Origin-проверка вообще пройти; нет rate-limit на анонимный OAuth code-exchange (два исходящих вызова к Яндексу на запрос); open redirect через `//evil.com` в пост-логин редиректе; сессии продлеваются бесконечно при регулярном refresh, нет способа отозвать чужую сессию при revoke; `YANDEX_CLIENT_SECRET` хранится под именем с префиксом `VITE_` — на грани случайной публикации в клиентский бандл; дефолтные креды БД в dev-compose с портом наружу; `chatID` не URL-энкодится в `panelApi.js` в отличие от `accessApi.js` (сегодня безвредно, т.к. ID серверные).

---

## Ремедиация критичных находок Wave 10 (C1-C4, SEC-11-01)

**Status:** Done
**Commit:** (см. коммит этой волны)
**Agent:** лид напрямую (не отдельная задача пайплайна) — пользователь выбрал «Ремедиация критичных сейчас, потом Wave 11» в ответ на сводку по Task 10/11
**Summary:** Устранены все четыре critical-находки Task 10 (C1-C4) и critical-находка Task 11 (SEC-11-01), плюс попутно найденные при Point B-ревью этой самой ремедиации проблемы. Independent Point B: code-reviewer (1 critical + 4 major + 9 minor) и security-auditor (SEC-11-01 независимо переверифицирован живым воспроизведением на MariaDB, C1/C3/C2 подтверждены, 1 major + 8 minor) — оба ревью выполнены на новом коде, а не на прототипе.

**SEC-11-01 — эскалация привилегий через порядок миграций (было critical):**
- Новая `server/migrations/006_admin_identity_reassert.up.sql`: после того как 005 сделала сравнение `users.id` байт-точным, снимает `role='admin'` (и, после доработки по ревью, `has_access`) с любой строки, чей `id` не совпадает байт-в-байт ни с одним из двух настоящих логинов, и переустанавливает обоих настоящих администраторов. Живо воспроизведено лидом (посеяна строка-коллизия `rogogdbd`, применены реальные файлы 004→005 → `rogogdbd` осталась ADMIN, `RoGogDBD` после входа получила `role='user'`) и независимо переверифицировано security-auditor'ом с нуля на отдельной живой базе, включая проверку под коллацией соединения приложения (`utf8mb4_unicode_ci` из `internal/db/db.go`, отличается от CLI/тестового DSN) — не влияет на байт-точность сравнения (тип колонки важнее клиентской collation литерала).
- `server/migrations/migrate_test.go` (новый): `TestAdminSeed_SurvivesCollisionUnderLegacyCollation` (ручное применение файлов, контролируемое окно между 005 и 006 для симуляции self-grant) и `TestAdminSeed_SurvivesCollisionUnderLegacyCollation_ViaRun` (та же проверка через настоящую точку входа `migrations.Run`, включая идемпотентность повторного вызова и запись в `schema_migrations`) — второй тест добавлен по замечанию security-auditor'а (REM-04): первый доказывает верность SQL, второй — что реальный код приложения действительно применяет его в предполагаемом порядке. Новый env `TEST_MIGRATION_ADMIN_DSN` (права CREATE/DROP DATABASE, которых нет у прикладного пользователя `TEST_DB_DSN`), подключён в `test.yml` через уже доступные там CI-only root-креды сервис-контейнера.
- Все мутационные проверки лида подтверждены: тест падает без 006, падает без сброса `has_access`, ловит откат `defer`/`Cleanup`.

**C1 — same-origin fallback ломается обоими прокси проекта (было critical):**
- `docker-compose.yml`: `CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:5173}` — явный allowlist обходит сравнение с `r.Host` целиком, а не пытается его починить. security-auditor подтвердил: `originAllowed` возвращает результат сверки с картой раньше, чем трогает `r.Host`, и что дефолт не может утечь в прод-конфиг (`deploy.yml` использует только `docker-compose.prod.yml`, где `CORS_ALLOWED_ORIGINS` без дефолта).
- `server/internal/config/config.go`: при пустом (после разбора CSV, а не по сырой строке — см. ниже) `CORS_ALLOWED_ORIGINS` и `COOKIE_SECURE=false` — предупреждение в лог на `APP_STORAGE=memory` (заведомо не прод), и **жёсткий отказ `Load()`** на любом другом хранилище (сигнал реального деплоя с настоящей БД). Второе — доработка по замечанию security-auditor'а (REM-03): изначальная версия только логировала и на проде тихо не мешала бы стартовать в опасной конфигурации.
- Голый Vite dev-прокси (`npm run dev` без docker-compose) сознательно не тронут — это отдельный локальный путь, уже покрытый существующим `.env`-конвейером `config.Load()` и примером в `README.md:173`; security-auditor подтвердил, что это не пропущенная часть того же C1, а действительно другой code path.

**C2/SEC-11-05 — утечка текста ошибок парсера (было critical/medium, обе аудита нашли независимо):**
- Новый общий хелпер `writeAPIDecodeError` в `http.go`; все четыре сайта в `http.go` (`handleUpsertSettings`, `handleCreateChat`, `handleCalculate`, `handleMarketFeedback`) и дублирующая инлайн-копия в `access.go` переведены на него. Заодно поправлен свободный текст 503 в `handleMarketFeedback` (`m9` из Task 10) на тот же слаг `service_unavailable`, что и везде.
- Новые тесты: `TestHandleUsers_DecodeError_NoInternalTextLeak` (3 из 4 сайтов через реальную `BuildRoutes` — четвёртый, `market-feedback`, недостижим с `deepseek=nil` в общей фикстуре, тот же пробел уже зафиксирован в отчёте Task 12) и `TestWriteAPIDecodeError_LogsOriginalError` (добавлен по замечанию code-review: исходный тест проверял только отсутствие утечки в ответе, не то, что ошибка реально попадает в лог). security-auditor подтвердил: ровно пять сайтов декодирования во всём дереве, все пять — через `writeAPIDecodeError`, шестого не осталось.

**C3 — `generateToken()` глотает ошибку `crypto/rand` (было critical) — расследовано, оказалось ложным срабатыванием:**
- `go doc crypto/rand Read` на `golang:1.25-alpine` (версия модуля — `go 1.25.5`): «It never returns an error… crashes the program irrecoverably if an error is returned» — начиная с Go 1.24 пустой/нулевой токен из этой функции структурно недостижим (либо настоящий случайный результат, либо падение процесса раньше, чем что-либо будет возвращено). Совпадает с независимым выводом самого Task 11 (`verified_clean`). Логика не менялась, добавлен только поясняющий комментарий — сode-reviewer перепроверил напрямую по исходнику `crypto/rand` в контейнере и подтвердил оба пути (`*reader` и `Reader`-обёртку).

**Находки, обнаруженные при Point B-ревью самой ремедиации (не из Task 10/11) — все исправлены:**
- **critical (code-review)** — `migrate_test.go` утекал одноразовые базы: `defer admin.Close()` выполнялся раньше `t.Cleanup`-а с `DROP DATABASE`, ошибка глоталась `_, _ =`. На деле утекло 9 баз `migration_race_*` за время верификации (лид + оба ревьюера). Исправлено: оба cleanup'а — через `t.Cleanup`, в порядке, гарантирующем DROP раньше Close (LIFO); ошибка DROP теперь падает тестом, а не молчит. Утечки удалены вручную, повторных не возникло.
- **major (code-review)** — предупреждение в `config.go` сверялось с сырой строкой `cfg.AllowedOrigins`, а не с результатом `splitCSV` (который реально строит allowlist) — пропускало бы значения вроде `" "`/`","`. Исправлено: локальная копия правила разбора (`hasNonEmptyCSVEntry`), протестировано отдельным кейсом.
- **major/REM-03 (security-audit)** — см. C1 выше, фикс дополнен жёстким отказом на не-memory хранилище.
- **minor (code-review)** — 006 не сбрасывала `has_access` у демоутнутой строки: пока строка ошибочно была админом, её владелец мог сам себе выставить `has_access=true` через API и сохранить обычный доступ после фикса роли. Исправлено, покрыто тестом (симуляция self-grant в контролируемом окне между 005 и 006).
- **REM-04 (security-audit)** — добавлен второй тест через `migrations.Run` (см. выше).

**Notes carried forward (не в объёме этой ремедиации, зафиксировано, не забыто):**
- Task 10 M4 (`docker-compose.yml` не задаёт `APP_STORAGE`, dev-стек работает на `MemoryRepository`) и отсутствие `YANDEX_*` в том же файле — не исправлены: это отдельная находка (не C1/не critical), а добавление реальных Yandex OAuth credentials лиду недоступно. C1's фикс делает dev-стек корректным по Origin, но не end-to-end рабочим для логина — это не regression этой ремедиации, а уже существовавший, отдельно задокументированный пробел.
- REM-05 (security-audit) — сортировка миграций смешивает `0001_`/`00N_`-префиксы разной ширины; будущий `0010_*.up.sql` отсортировался бы раньше `001_`. Структурный риск всей системы миграций, не специфичный для этой ремедиации — не тронуто.
- REM-06/07/08/09 (security-audit, все minor) — нет `MaxBytesReader` на пяти decode-сайтах; `ResolveSession` не логирует ошибки репозитория; `market-feedback`-путь и новое предупреждение конфигурации не покрыты тестами (второе — уже закрыто добавленным `TestLoad_OriginFallbackGuard`, первое — тот же известный пробел, что и раньше); 12 находок Task 10/11, оставшихся вне объёма «критичное сейчас» (nginx security headers, rate limiting на OAuth, open redirect, sliding sessions, `VITE_`-префикс у серверного секрета, npm/govulncheck advisories, дефолтные dev-креды БД) — остаются в бэклоге, каждая с точным описанием и предложенным фиксом в исходных отчётах `task-10/code-audit.md` и `task-11/security-auditor-1.json`.

**Verification:**
- `go build ./...`, `go vet ./...` — чисто.
- `go test ./...` (полный набор, живая MariaDB + `TEST_MIGRATION_ADMIN_DSN`) — все пакеты `ok`, включая оба новых теста миграций и новый `TestLoad_OriginFallbackGuard`.
- Мутационные проверки лида: тест миграции падает без 006; падает без сброса `has_access`; падает при откате любого из четырёх `writeAPIDecodeError`; ни одна временная база не осталась после прогона.
- Независимая мутационная проверка security-auditor'а: тест на SEC-11-01 падает при удалении 006 из копии дерева, проходит при восстановлении.
- YAML (`test.yml`, `docker-compose.yml`) — парсится чисто.

---

## Task 13: Pre-deploy QA

**Status:** Done
**Agent:** pre-deploy-qa (аудитная задача, отдельного исполнителя-ревьюера нет)
**Summary:** Приёмочное тестирование Phase 1 против полного списка Acceptance Criteria tech-spec.md и US-1..US-17 user-spec.md. **Вердикт: PASS.** 42 критерия: 40 passed, 0 failed, 2 not_verifiable (обе ожидаемые — секрет на живом хосте статически не проверить, US-5 вне объёма Phase 1). Полный отчёт: [logs/working/task-13/qa-report.json](logs/working/task-13/qa-report.json), verbose-лог тестов: [logs/working/task-13/gotest-v.log](logs/working/task-13/gotest-v.log).

**Ключевая проверка задачи — не спутать «прошло» с «молча пропущено».** `go test ./... -v -count=1` против живой MariaDB: **397 PASS, 0 FAIL, 0 SKIP** по всем 7 пакетам. Поставлен негативный контроль: тот же прогон без `TEST_DB_DSN`/`TEST_MIGRATION_ADMIN_DSN` даёт 26 строк `SKIP` — подтверждает, что зелёный прогон действительно отличим от тихого пропуска, а не просто получен на удачу. Все 6 «несущих» тестов из исходного списка tasks/13.md плюс оба новых теста миграции 006 (добавлены ремедиацией, не упомянуты в исходном тексте задачи, но явно включены как несущие лидом при постановке задачи) показали PASS.

**Независимая переверификация claims ремедиации** (не приняты на веру, передоказаны заново): чистый прогон 001→006 на свежей MariaDB даёт ровно 2 админа; повторное применение 004 и 006 идемпотентно; SEC-11-01 воспроизведён с нуля на отдельной базе (после 004 одной — `rogogdbd` захватывает admin, `RoGogDBD` не существует; после 006 — `rogogdbd` разжалована с `has_access=0`, настоящий админ восстановлен, count=2); утечек временных баз `migration_race_*`/`qa13_*` не осталось.

**Находки:**
- **QA-13-01 (major, не дефект кода — операционное условие).** Ремедиация C1 добавила жёсткий отказ `config.Load()` при пустом `CORS_ALLOWED_ORIGINS` + `COOKIE_SECURE=false` на любом хранилище, кроме `memory`. `deploy.yml` жёстко прописывает `APP_STORAGE: mysql`, то есть прод всегда попадает в эту ветку, а оба значения приходят из необязательных GitHub-секретов. **Если хотя бы один не выставлен — `poshivon_app` не стартует вовсе**, а не тихо ловит 403 (это и есть цель фикса), но это новое условие для деплоя, которого не было на момент последней проверки пайплайна Task 9. Не код-дефект — поведение отработало ровно так, как спроектировано (`TestLoad_OriginFallbackGuard` подтверждает), но чеклист перед Wave 12 должен включить: **проверить, что секреты `CORS_ALLOWED_ORIGINS` (реальный прод-origin) и `COOKIE_SECURE=true` выставлены в GitHub** — иначе деплой пройдёт, а контейнер не поднимется.
- QA-13-02 (minor) — три критерия чисто клиентские (state-mismatch, охрана эффектов без доступа, скрытие пункта «Пользователи») проверены только чтением кода — в проекте нет клиентского test runner'а (уже зафиксировано Task 12), у каждого есть независимое серверное принуждение.
- QA-13-03 (minor) — `schema_migrations` в `poshivon_test` фиксирует только `001_auth_sessions`, хотя объекты всех шести миграций на месте — артефакт того, что тестовая фикстура (и `test.yml`) накатывают файлы клиентом `mariadb` напрямую, а не через `migrations.Run`; путь `Run`'а отдельно покрыт `TestAdminSeed_SurvivesCollisionUnderLegacyCollation_ViaRun`. Не баг продукта.

**Отложено на пост-деплой (deferredToPostDeploy в отчёте, не блокирует Wave 12):** живая проверка `CONTACT_EMAIL` на банере; US-5 (Phase 2, Task 18 заберёт); рендер-поведение панели глазами живого браузера (US-1/2/4/7/8, TS-19/20 — по сути шаги 9-11 из «Agent verifies» user-spec.md, назначенные Playwright MCP); подтверждение пользователем, что два засеянных админ-логина — те самые люди, плюс повторная живая проверка отсутствия строк-коллизий на проде после деплоя.

**Notes carried forward:**
- Бэклог находок Task 10/11, не вошедших в «критичное сейчас» (security-заголовки nginx, rate-limit на OAuth, open redirect, sliding sessions, `VITE_`-префикс серверного секрета, npm/govulncheck advisories, дефолтные dev-креды БД) — остаётся зафиксированным в исходных отчётах, не является частью Phase-1 acceptance criteria, не блокирует эту волну.

**Verification:** см. полный отчёт — 397/397 тестов, негативный контроль на skip-хазард, независимая передоказанная верификация ремедиации, полный проход по 42 критериям с explicit evidence на каждый.
